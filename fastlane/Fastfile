# fastlane/Fastfile
require "plist"
require "base64"
require "tempfile"

default_platform(:ios)

ROOT_DIR = File.expand_path("..", __dir__)
ENV["SPACESHIP_CONNECT_API_IN_HOUSE"] ||= "false"

# -------------------------------------------------
# 🔑  Build App Store Connect API-key hash
# -------------------------------------------------
def app_store_api_key_hash
  api_key_id = ENV["APP_STORE_CONNECT_API_KEY_ID"]
  issuer_id  = ENV["APP_STORE_CONNECT_API_ISSUER_ID"] || ENV["APP_STORE_CONNECT_API_KEY_ISSUER_ID"]
  key_base64 = ENV["APP_STORE_CONNECT_API_KEY_BASE64"]
  raw_key    = ENV["APP_STORE_CONNECT_API_KEY"]

  if api_key_id.to_s.strip.empty? || issuer_id.to_s.strip.empty?
    UI.user_error!("APP_STORE_CONNECT_API_KEY_ID and APP_STORE_CONNECT_API_ISSUER_ID must be set")
  end

  if key_base64.to_s.strip.empty? && raw_key.to_s.strip.empty?
    UI.user_error!("Either APP_STORE_CONNECT_API_KEY_BASE64 or APP_STORE_CONNECT_API_KEY must be set")
  end

  {
    key_id:    api_key_id,
    issuer_id: issuer_id,
    key:       key_base64.to_s.strip.empty? ? raw_key : Base64.decode64(key_base64),
    in_house:  false
  }
end

# -------------------------------------------------
# 📦  Shared Xcode build options
# -------------------------------------------------
def ios_build_options
  workspace_path = File.join(ROOT_DIR, "ios", "JunoNative.xcworkspace")
  project_path   = File.join(ROOT_DIR, "ios", "JunoNative.xcodeproj")

  has_workspace = File.exist?(workspace_path)
  has_project   = File.exist?(project_path)

  UI.user_error!("No workspace or project found in ios/") unless has_workspace || has_project

  export_options_plist = File.join(ROOT_DIR, "ios", "exportOptions.plist")
  UI.user_error!("exportOptions.plist missing at #{export_options_plist}") unless File.exist?(export_options_plist)
  plist_export_options = Plist.parse_xml(export_options_plist) || {}

  # Force team ID if passed from CI
  unless ENV["APPLE_TEAM_ID"].to_s.strip.empty?
    plist_export_options["teamID"] = ENV["APPLE_TEAM_ID"]
  end

  profile_name   = ENV["IOS_PROVISIONING_PROFILE_NAME"]
  app_identifier = ENV["APP_IDENTIFIER"]

  if profile_name.to_s.strip.empty?
    # Let Xcode handle it
    plist_export_options["signingStyle"] = "automatic"
  elsif app_identifier.to_s.strip.empty?
    UI.user_error!("IOS_PROVISIONING_PROFILE_NAME set but APP_IDENTIFIER missing")
  else
    plist_export_options["signingStyle"] = "manual"
    plist_export_options["provisioningProfiles"] ||= {}
    plist_export_options["provisioningProfiles"][app_identifier] = profile_name
  end

  {
    workspace:         has_workspace ? workspace_path : nil,
    project:           has_workspace ? nil            : project_path,
    scheme:            "JunoNative",
    configuration:     "Release",
    clean:             true,
    output_directory:  File.join(ROOT_DIR, "fastlane", "build", "ios"),
    output_name:       ENV["IOS_OUTPUT_NAME"] || "JunoNative.ipa",
    export_options:    plist_export_options,
    result_bundle:     true,
    buildlog_path:     File.join(ROOT_DIR, "fastlane", "logs"),
    export_xcargs:     "DEVELOPMENT_TEAM=#{ENV['APPLE_TEAM_ID']}"
  }.compact
end

# =================================================
# 🚀  Lanes
# =================================================
platform :ios do
  # ---------- Prep / native bits ----------
  desc "Run unit tests for the Juno DSP core (CMake / CTest)"
  lane :dsp_tests do
    sh("cmake -S . -B fastlane/build/cmake -DCMAKE_BUILD_TYPE=Debug")
    sh("cmake --build fastlane/build/cmake --config Debug --target juno_tests")
    sh("ctest --test-dir fastlane/build/cmake --output-on-failure")
  end

  desc "Compile native C++ DSP engine (Release)"
  lane :compile_dsp_engine do
    build_dir = File.join(ROOT_DIR, "fastlane", "build", "cmake")
    sh("cmake -S #{ROOT_DIR} -B #{build_dir} -DCMAKE_BUILD_TYPE=Release")
    sh("cmake --build #{build_dir} --config Release")
  end

  desc "Generate / compile React Native TurboModules"
  lane :compile_turbo_modules do
    script = File.join(ROOT_DIR, "scripts", "compile-turbo-modules.sh")
    UI.user_error!("Missing #{script}") unless File.exist?(script)
    sh("bash #{script}")
  end

  private_lane :install_cocoapods do
    ios_dir = File.join(ROOT_DIR, "ios")
    podfile = File.join(ios_dir, "Podfile")
    UI.user_error!("Podfile missing in ios/") unless File.exist?(podfile)
    Dir.chdir(ios_dir) do
      sh("bundle exec pod install --repo-update")
    end
  end

  private_lane :prepare_build_assets do
    compile_dsp_engine
    compile_turbo_modules
  end

  private_lane :compile_swift_sources do
    ws = File.join(ROOT_DIR, "ios", "JunoNative.xcworkspace")
    pr = File.join(ROOT_DIR, "ios", "JunoNative.xcodeproj")
    xcpretty_available = system("which xcpretty >/dev/null 2>&1")
    pretty = xcpretty_available ? " | xcpretty --no-color" : ""

    cmd =
      if File.exist?(ws)
        %Q[xcodebuild -workspace "#{ws}" -scheme JunoNative -configuration Release -sdk iphoneos -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build]
      elsif File.exist?(pr)
        %Q[xcodebuild -project "#{pr}" -scheme JunoNative -configuration Release -sdk iphoneos -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build]
      else
        UI.user_error!("No xcworkspace/xcodeproj found under ios/")
      end

    sh("set -o pipefail && #{cmd}#{pretty}")
  end

  # ---------- Local build ----------
  desc "Local Release build (no upload)"
  lane :build do
    prepare_build_assets
    install_cocoapods
    compile_swift_sources
    build_app(ios_build_options)
  end

  # -------------------------------------------------
  # 🔍  Verification (API key + p12 + profile)
  # -------------------------------------------------
  desc "Verify ASC credentials, p12 & provisioning profile (no build)"
  lane :verify_appstore_connect do
    UI.header("🔍 Verifying ASC credentials, certificate & profile")

    # Basic env sanity – these MUST be present in CI
    %w[
      APPLE_ID
      APP_IDENTIFIER
      APPLE_TEAM_ID
      APP_STORE_CONNECT_API_KEY_ID
      APP_STORE_CONNECT_API_ISSUER_ID
      IOS_DIST_CERT_BASE64
      IOS_DIST_CERT_PASSWORD
    ].each do |v|
      UI.user_error!("Missing #{v}") if ENV[v].to_s.strip.empty?
    end

    # 1) ASC authentication --------------------------------------------------
    api_key = app_store_api_key_hash

    require "spaceship"
    token = Spaceship::ConnectAPI::Token.create(**api_key)
    Spaceship::ConnectAPI.token = token

    apps = Spaceship::ConnectAPI::App.all
    UI.success("✅ Authenticated — #{apps.size} apps in this account")

    if (bundle_id = ENV["APP_IDENTIFIER"]).to_s.strip != ""
      app = apps.find { |a| a.bundle_id == bundle_id }
      if app
        UI.success("✅ Found app #{app.name} (#{bundle_id})")
      else
        UI.important("⚠️ No ASC app with bundle id #{bundle_id}")
      end
    end

    # 2) Validate the p12 itself --------------------------------------------
    UI.header("🔐 Verifying distribution certificate (.p12)")

    decoded = Base64.decode64(ENV["IOS_DIST_CERT_BASE64"])
    UI.user_error!("Decoded p12 is empty – check IOS_DIST_CERT_BASE64") if decoded.to_s.empty?

    Tempfile.create(["dist", ".p12"]) do |tmp|
      tmp.binmode
      tmp.write(decoded)
      tmp.flush

      # We only check that the password works & structure is valid.
      sh("openssl pkcs12 -in '#{tmp.path}' -nokeys -passin pass:#{ENV['IOS_DIST_CERT_PASSWORD']} -info >/dev/null 2>&1")
    end

    UI.success("✅ p12 structure and password are valid")

    # 3) Provisioning profile via sigh --------------------------------------
    UI.header("🧾 Verifying provisioning profile via sigh")

    profile_path = sigh(
      app_identifier: ENV["APP_IDENTIFIER"],
      api_key:        api_key,
      adhoc:          false,
      readonly:       false,
      skip_install:   true
    )

    # older fastlane populates SIGH_PROFILE_PATH; be defensive
    if profile_path.to_s.strip.empty?
      profile_path = Actions.lane_context[Fastlane::Actions::SharedValues::SIGH_PROFILE_PATH]
    end

    UI.user_error!("sigh did not return a provisioning profile path") if profile_path.to_s.strip.empty?

    profile_name = File.basename(profile_path, ".mobileprovision")

    # Expose to subsequent lanes (build_app)
    ENV["SIGH_PROFILE_PATH"]          = profile_path
    ENV["IOS_PROVISIONING_PROFILE_NAME"] = profile_name

    UI.success("✅ Provisioning profile downloaded: #{profile_name}")
    UI.success("🎉 Verification completed.")
  end

  # ---------- CI lane ----------
  desc "CI: build JunoNative and upload to TestFlight"
  lane :ios_ci do
    # 1) Make sure ASC / cert / profile all look good
    verify_appstore_connect

    # 2) Build native bits + RN + Pods
    prepare_build_assets
    install_cocoapods
    compile_swift_sources

    # 3) Build IPA using the profile name set by verify_appstore_connect
    build_app(ios_build_options)

    # 4) Upload to TestFlight
    upload_to_testflight(
      api_key:        app_store_api_key_hash,
      app_identifier: ENV["APP_IDENTIFIER"],
      team_id:        ENV["APPLE_TEAM_ID"],
      skip_waiting_for_build_processing: false,
      distribute_external:              false
    )
  end
end
